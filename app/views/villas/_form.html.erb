<%= simple_form_for [@villa] do |f| %>
  <%= f.error_notification %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :price, input_html: { min: 0, step: "any"}%>
  <%= f.input :photo, as: :attachinary %>

  <%= f.submit "add a villa", class: "form-btn btn btn-primary" %>
<% end %>
